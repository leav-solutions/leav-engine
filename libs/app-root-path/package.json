{
  "name": "@leav/app-root-path",
  "version": "1.0.0-beta6",
  "description": "Module to get app root path",
  "scripts": {
    "test": "jest -c jest-config.json",
    "test:ci": "jest -c jest-config.json",
    "test:commit": "jest -c jest-config.json --maxWorkers=50% --findRelatedTests ",
    "prepublishOnly": "npm run build",
    "build": "tsc --build",
    "tscheck": "tsc --noEmit -p .",
    "tscheck:watch": "tsc -w --noEmit -p ."
  },
  "main": "dist/index",
  "keywords": [
    "config",
    "configuration",
    "settings"
  ],
  "author": "",
  "license": "LGPL3",
  "devDependencies": {
    "@types/jest": "29.0.2",
    "eslint": "7.32.0",
    "jest": "29.0.3",
    "prettier": "2.7.1",
    "ts-jest": "29.0.1",
    "typescript": "4.8.3"
  },
  "dependencies": {
    "app-root-path": "3.0.0"
  },
  "stableVersion": "1.0.0"
}
