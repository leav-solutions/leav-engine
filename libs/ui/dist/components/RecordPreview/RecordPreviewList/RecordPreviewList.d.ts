/// <reference types="react" />
import {IRecordPreviewProps} from '../_types';
declare function RecordPreviewList({label, color, image, size, style, simplistic}: IRecordPreviewProps): JSX.Element;
export default RecordPreviewList;
