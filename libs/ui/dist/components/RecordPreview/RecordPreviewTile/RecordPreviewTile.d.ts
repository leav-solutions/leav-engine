/// <reference types="react" />
import {IRecordPreviewProps} from '../_types';
declare function RecordPreviewTile({
    label,
    color,
    image,
    style,
    imageStyle,
    placeholderStyle
}: IRecordPreviewProps): JSX.Element;
export default RecordPreviewTile;
