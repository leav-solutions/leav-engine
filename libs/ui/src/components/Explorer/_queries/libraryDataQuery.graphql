query ExplorerLibraryDetails($libraryId: ID!) {
    libraries(filters: {id: [$libraryId]} ) {
        list {
            id
            label
            behavior
            permissions {
                create_record
            }
        }
    }
}
