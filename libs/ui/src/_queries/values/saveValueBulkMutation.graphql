mutation SAVE_VALUE_BULK(
    $libraryId: ID!
    $recordsFilters: [RecordFilterInput]!
    $attributeId: ID!
    $mapping: [SaveValueBulkMappingInput!]!
) {
    saveValueBulk(libraryId: $libraryId, recordsFilters: $recordsFilters, attributeId: $attributeId, mapping: $mapping)
}
