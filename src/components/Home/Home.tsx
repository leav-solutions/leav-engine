import React from 'react';
import {Header} from 'semantic-ui-react';

function Home(): JSX.Element {
    return (
        <div>
            <Header>Home</Header>
        </div>
    );
}

export default Home;
