subscription subscribeToUserTasks($filters: TaskFiltersInput) {
    task(filters: $filters) {
        id
        status
        label
        created_at
        startedAt
        completedAt
        progress {
            description
            percent
        }
        link {
            url
        }
    }
}