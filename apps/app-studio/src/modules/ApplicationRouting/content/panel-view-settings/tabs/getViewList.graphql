query GetViewList($libraryId: String!) {
    views(library: $libraryId) {
        # TODO: change to viewV2 when data is ready and need to change
        list {
            id
            label
            shared
            created_by {
                id
            }
        }
    }
}
