query GetViewList($libraryId: ID!, $origin: String) {
    viewsV2(library: $libraryId, origin: $origin) {
        list {
            id
            label
            shared
            origin
            created_by {
                id
            }
        }
    }
}
