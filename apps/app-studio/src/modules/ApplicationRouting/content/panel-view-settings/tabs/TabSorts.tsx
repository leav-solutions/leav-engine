import {KitEmpty} from 'aristid-ds';

export const TabSorts = ({canEditAdminView}: {canEditAdminView: boolean}) => (
    // TODO (admin): configure available attributes for sorts
    // TODO (user): list active sorts from current SerializedView, add/remove from available attributes
    <KitEmpty description="Work in progress" />
);
