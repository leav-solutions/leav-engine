import {KitEmpty} from 'aristid-ds';

export const TabFilters = () => (
    // TODO (admin): configure available attributes for filters
    // TODO (user): list active filters from current SerializedView, add/remove/pin from available attributes
    <KitEmpty description="Work in progress" />
);
