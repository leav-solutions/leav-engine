query GetPermissionEditViewOnLibrary($libraryId: ID!) {
    libraries(filters: {id: [$libraryId]}) {
        list {
            id
            permissions {
                admin_library
            }
        }
    }
}
